# SynthWatch — handover inventory (GENERATED)

> **Do not hand-edit.** Regenerate with `scripts/generate-handover-inventory.sh`.
> Every fact below is read from the LIVE system at generation time — never from docs or memory.
> Each section prints the exact command that produced it, so any single line can be re-verified.

| | |
|---|---|
| **Generated at (UTC)** | `2026-07-31T12:13:59Z` |
| **Generated by** | `scripts/generate-handover-inventory.sh` |
| **Source commit** | `53a4057` |
| **Azure subscription** | SynthWatch (`505a01eb-9a5f-4212-a293-ff82a68d0d03`) |
| **Tenant** | `087ee734-7aed-45e9-93ba-e1bb35e3e572` |
| **Resource group** | `synthwatch-rg` |

**Staleness gate:** `.github/workflows/inventory-freshness.yml` fails once this file is older than
its threshold, so it cannot silently rot the way a hand-written document does.

---

## 1. Azure resources

### 1.1 Every resource in `synthwatch-rg`

```console
$ az resource list -g synthwatch-rg --query "[].{type:type,name:name,location:location}" -o tsv | sort
```

| Type | Name | Region |
|---|---|---|
| `Microsoft.App/jobs` | `synthwatch-b2c-probe-job` | eastus2 |
| `Microsoft.App/jobs` | `synthwatch-migrate-job` | eastus2 |
| `Microsoft.App/jobs` | `synthwatch-narrative-job` | eastus2 |
| `Microsoft.App/jobs` | `synthwatch-reconcile-job` | eastus2 |
| `Microsoft.App/jobs` | `synthwatch-retention-job` | eastus2 |
| `Microsoft.App/jobs` | `synthwatch-rollup-job` | eastus2 |
| `Microsoft.App/jobs` | `synthwatch-runner-job` | eastus2 |
| `Microsoft.App/jobs` | `synthwatch-runner-job-centralus` | centralus |
| `Microsoft.App/jobs` | `synthwatch-runner-job-westus2` | westus2 |
| `Microsoft.App/jobs` | `synthwatch-sandbox` | eastus2 |
| `Microsoft.App/managedEnvironments` | `synthwatch-env-centralus` | centralus |
| `Microsoft.App/managedEnvironments` | `synthwatch-env-e2` | eastus2 |
| `Microsoft.App/managedEnvironments` | `synthwatch-env-westus2` | westus2 |
| `Microsoft.CognitiveServices/accounts` | `synthwatch-aoai` | eastus2 |
| `Microsoft.Communication/CommunicationServices` | `synthwatch-acs` | global |
| `Microsoft.Communication/EmailServices` | `synthwatch-email` | global |
| `Microsoft.Communication/EmailServices/Domains` | `synthwatch-email/AzureManagedDomain` | global |
| `Microsoft.ContainerRegistry/registries` | `synthwatcholey0620` | eastus |
| `Microsoft.DBforPostgreSQL/flexibleServers` | `synthwatch-pg-e2` | eastus2 |
| `microsoft.insights/actiongroups` | `Application Insights Smart Detection` | global |
| `Microsoft.Insights/actiongroups` | `synthwatch-fleet-alerts` | global |
| `Microsoft.Insights/components` | `synthwatch-api-ai` | eastus2 |
| `Microsoft.Insights/scheduledqueryrules` | `synthwatch-fleet-guard-refusal` | eastus2 |
| `Microsoft.Insights/scheduledqueryrules` | `synthwatch-fleet-heartbeat-absent` | eastus2 |
| `Microsoft.ManagedIdentity/userAssignedIdentities` | `synthwatch-runner-id` | eastus2 |
| `Microsoft.ManagedIdentity/userAssignedIdentities` | `synthwatch-sandbox-id` | eastus2 |
| `Microsoft.OperationalInsights/workspaces` | `synthwatch-api-law` | eastus2 |
| `Microsoft.OperationalInsights/workspaces` | `synthwatch-logs-e2` | eastus2 |
| `Microsoft.Storage/storageAccounts` | `stcq3qolcvktauq` | eastus2 |
| `Microsoft.Storage/storageAccounts` | `synthwatche2046f4733` | eastus2 |
| `Microsoft.Storage/storageAccounts` | `synthwatche24e33105c` | eastus2 |
| `Microsoft.Web/serverFarms` | `synthwatch-api-plan` | eastus2 |
| `Microsoft.Web/sites` | `synthwatch-api` | eastus2 |

_33 resources._

### 1.2 Container Apps jobs — image, size, timeout, trigger

`replicaTimeout` is the per-execution wall clock. A runner tick runs its due checks SEQUENTIALLY
inside ONE execution and shares this budget — so it is a capacity fact, not just a setting.

```console
$ az containerapp job list -g synthwatch-rg --query '[].{...}' -o tsv
```

| Job | Region | CPU / Mem | replicaTimeout | Trigger | Cron | Image tag |
|---|---|---|---|---|---|---|
| `synthwatch-b2c-probe-job` | East US 2 | 1.0 / 2Gi | 600s | Manual | `None` | `latest` |
| `synthwatch-migrate-job` | East US 2 | 0.25 / 0.5Gi | 600s | Manual | `None` | `53a40576905b695f497916b0c3f1718925d0e3e1` |
| `synthwatch-narrative-job` | East US 2 | 0.25 / 0.5Gi | 600s | Schedule | `30 0 * * *` | `53a40576905b695f497916b0c3f1718925d0e3e1` |
| `synthwatch-reconcile-job` | East US 2 | 0.25 / 0.5Gi | 600s | Schedule | `0 * * * *` | `53a40576905b695f497916b0c3f1718925d0e3e1` |
| `synthwatch-retention-job` | East US 2 | 0.25 / 0.5Gi | 600s | Schedule | `45 0 * * *` | `53a40576905b695f497916b0c3f1718925d0e3e1` |
| `synthwatch-rollup-job` | East US 2 | 0.25 / 0.5Gi | 600s | Schedule | `7 0 * * *` | `53a40576905b695f497916b0c3f1718925d0e3e1` |
| `synthwatch-runner-job` | East US 2 | 2.0 / 4Gi | 660s | Schedule | `*/5 * * * *` | `53a40576905b695f497916b0c3f1718925d0e3e1` |
| `synthwatch-runner-job-centralus` | Central US | 2.0 / 4Gi | 660s | Schedule | `*/5 * * * *` | `53a40576905b695f497916b0c3f1718925d0e3e1` |
| `synthwatch-runner-job-westus2` | West US 2 | 2.0 / 4Gi | 660s | Schedule | `*/5 * * * *` | `53a40576905b695f497916b0c3f1718925d0e3e1` |
| `synthwatch-sandbox` | East US 2 | 1.0 / 2Gi | 180s | Manual | `None` | `53a40576905b695f497916b0c3f1718925d0e3e1` |

**Image repository (all jobs):** `synthwatcholey0620.azurecr.io/synthwatch-runner`

★ All jobs must run the SAME image tag. A tag that differs from the others is a job CD did not roll
(the drift class `scripts/deploy_test.sh` guards). Distinct tags live right now:
  - `53a40576905b695f497916b0c3f1718925d0e3e1`
  - `latest`

### 1.3 Managed identities and their EXACT live role assignments

Read LIVE (`az role assignment list --assignee`), not from the bicep template — so a grant that
exists in Azure but not in code, or vice versa, shows up here as it really is.

```console
$ az identity list -g synthwatch-rg; az role assignment list --assignee <principalId> --all
```


**`synthwatch-runner-id`** — principalId `5ca727ad-06a2-42a9-b31c-4e7b9382ab96`, clientId `9015212f-3e39-4d70-9fa9-1d97f9662deb`

| Role | Scope |
|---|---|
| AcrPull | `/resourcegroups/synthwatch-rg/providers/Microsoft.ContainerRegistry/registries/synthwatcholey0620` |
| Cognitive Services OpenAI User | `/resourceGroups/synthwatch-rg/providers/Microsoft.CognitiveServices/accounts/synthwatch-aoai` |
| Container Apps Jobs Operator | `/resourcegroups/synthwatch-rg/providers/Microsoft.App/jobs/synthwatch-runner-job` |
| Container Apps Jobs Operator | `/resourcegroups/synthwatch-rg/providers/Microsoft.App/jobs/synthwatch-runner-job-centralus` |
| Container Apps Jobs Operator | `/resourcegroups/synthwatch-rg/providers/Microsoft.App/jobs/synthwatch-runner-job-westus2` |
| Cost Management Reader | `/resourcegroups/synthwatch-rg` |

**`synthwatch-sandbox-id`** — principalId `a98353dd-4b65-4fa1-8a85-9fe88f39ae6c`, clientId `13e7672e-7d00-4498-882b-18b9c4e5f540`

| Role | Scope |
|---|---|
| AcrPull | `/resourcegroups/synthwatch-rg/providers/Microsoft.ContainerRegistry/registries/synthwatcholey0620` |
| Storage Blob Data Contributor | `/resourcegroups/synthwatch-rg/providers/Microsoft.Storage/storageAccounts/synthwatche24e33105c/blobServices/default/containers/synthwatch-sandbox` |

### 1.4 Storage, containers and lifecycle rules

★ **Every** storage account in the group is enumerated, not just the first. There is more than one,
and they do NOT have the same retention posture — reporting only one would state a true fact about
the wrong account, which is precisely the class of wrong premise this document exists to end.

```console
$ az storage account list -g synthwatch-rg --query '[].name'   # then, per account: container list + management-policy show
```


#### `stcq3qolcvktauq`

- _No containers readable._
- **Lifecycle: NONE** — blobs in this account are retained indefinitely unless deleted by hand.

#### `synthwatche2046f4733`

- **Containers:** `synthwatch-artifacts`
  - `synthwatch-artifacts`: 0 blob(s) (capped at 1000 for speed)
- ⚠️ **Carries a `synthwatch-artifacts` container but is NOT the live artifact store** — no run
  references it. Treat as an ORPHAN from an earlier deploy: confirm it is empty, then do not
  recreate it in the destination. Naming alone does not make it the artifact account.
- **Lifecycle: NONE** — blobs in this account are retained indefinitely unless deleted by hand.

#### `synthwatche24e33105c`

- **Containers:** `synthwatch-artifacts`,`synthwatch-sandbox`
  - `synthwatch-artifacts`: 1000 blob(s) (capped at 1000 for speed)
  - `synthwatch-sandbox`: 0 blob(s) (capped at 1000 for speed)
- ★ **THE LIVE RUN-ARTIFACT ACCOUNT** — resolved from `runs.trace_url`, not guessed from the
  container name. Its lifecycle rules below are the retention clock for PII-bearing artifacts
  and must stay in step with the runner's row-retention window: rows and their blobs have to
  expire on the same clock, or purged rows leave dangling `trace_url`s.

| Lifecycle rule | Enabled | Prefixes | Delete after |
|---|---|---|---|
| `expire-artifacts` | true | synthwatch-artifacts/traces/, synthwatch-artifacts/run- | 90.0 days |
| `expire-sandbox-previews` | true | synthwatch-sandbox/ | 1.0 days |

### 1.5 Postgres, ACS and Azure OpenAI

```console
$ az postgres flexible-server list/db list; az communication list; az cognitiveservices account deployment list
```

**Postgres:** `synthwatch-pg-e2` — v16, Standard_B1ms (Burstable), 32GB, `synthwatch-pg-e2.postgres.database.azure.com`

  - Databases: `azure_maintenance`,`postgres azure_sys`,`synthwatch`

**Communication Services:** `synthwatch-acs` (data location: UnitedStates) — transactional alert email.

**Azure OpenAI:** `synthwatch-aoai` — powers RCA + report narratives. Absent config ⇒ both features go dark (opt-in by design).

| Deployment | Model | Version | SKU | Capacity |
|---|---|---|---|---|
| `gpt-5-mini` | gpt-5-mini | 2025-08-07 | GlobalStandard | 50 |

## 2. Secrets — names, scope, and where they live

★ **No values are printed anywhere in this file.** Names, scope and location only.

★ **THE TRANSFER RULE, and it is the reason this section is split by scope:**
> **Repository** secrets/variables move WITH the repo when it is transferred.
> **Environment** and **Organization** secrets DO NOT — they must be recreated in the destination.

### 2.1 Container Apps secrets, by job

These are the runtime credentials. Each is an ACA secret referenced by an env var; the VALUES live
only in the job definition and must be re-seeded in a new subscription.

```console
$ az containerapp job show -n <job> -g synthwatch-rg --query 'properties.configuration.secrets[].name'
```

| Job | ACA secret names | Env vars bound via secretRef |
|---|---|---|
| `synthwatch-runner-job-westus2` | database-url, storage-conn, acs-email-conn, vercel-bypass-token, cred-enc-key | DATABASE_URL, AZURE_STORAGE_CONNECTION_STRING, ACS_EMAIL_CONNECTION_STRING, VERCEL_BYPASS_TOKEN, CRED_ENC_KEY |
| `synthwatch-runner-job` | database-url, storage-conn, acs-email-conn, vercel-bypass-token, cred-enc-key | DATABASE_URL, AZURE_STORAGE_CONNECTION_STRING, ACS_EMAIL_CONNECTION_STRING, VERCEL_BYPASS_TOKEN, CRED_ENC_KEY |
| `synthwatch-migrate-job` | database-url | DATABASE_URL |
| `synthwatch-rollup-job` | database-url | DATABASE_URL |
| `synthwatch-narrative-job` | database-url | DATABASE_URL |
| `synthwatch-reconcile-job` | database-url | DATABASE_URL |
| `synthwatch-b2c-probe-job` | vercel-bypass-token, b2c-user, b2c-pass | VERCEL_BYPASS_TOKEN, B2C_TEST_USER, B2C_TEST_PASS |
| `synthwatch-retention-job` | database-url | DATABASE_URL |
| `synthwatch-sandbox` | — | — |
| `synthwatch-runner-job-centralus` | database-url, storage-conn, acs-email-conn, vercel-bypass-token, cred-enc-key | DATABASE_URL, AZURE_STORAGE_CONNECTION_STRING, ACS_EMAIL_CONNECTION_STRING, VERCEL_BYPASS_TOKEN, CRED_ENC_KEY |

### 2.2 GitHub secrets and variables, by scope

**`craigoley/synthwatch`**

| Repository secret | Updated | Transfers with repo? |
|---|---|---|
| `CLAUDE_CODE_OAUTH_TOKEN` | 2026-06-21 | ✅ yes |

| Repository variable | Value (non-secret) | Transfers with repo? |
|---|---|---|
| `AZURE_CLIENT_ID` | `b92b0ccc-d24e-4b6f-a6a0-c94de476cf1a` | ✅ yes |
| `AZURE_SUBSCRIPTION_ID` | `505a01eb-9a5f-4212-a293-ff82a68d0d03` | ✅ yes |
| `AZURE_TENANT_ID` | `087ee734-7aed-45e9-93ba-e1bb35e3e572` | ✅ yes |

_No deployment environments (so no environment-scoped secrets to recreate)._

**`craigoley/synthwatch-api`**

| Repository secret | Updated | Transfers with repo? |
|---|---|---|
| `CLAUDE_CODE_OAUTH_TOKEN` | 2026-06-21 | ✅ yes |

| Repository variable | Value (non-secret) | Transfers with repo? |
|---|---|---|
| `AZURE_CLIENT_ID` | `b92b0ccc-d24e-4b6f-a6a0-c94de476cf1a` | ✅ yes |
| `AZURE_SUBSCRIPTION_ID` | `505a01eb-9a5f-4212-a293-ff82a68d0d03` | ✅ yes |
| `AZURE_TENANT_ID` | `087ee734-7aed-45e9-93ba-e1bb35e3e572` | ✅ yes |
| `CORS_ALLOWED_ORIGINS` | `https://synthwatch-dashboard.vercel.app` | ✅ yes |

_No deployment environments (so no environment-scoped secrets to recreate)._

**`craigoley/synthwatch-dashboard`**

| Repository secret | Updated | Transfers with repo? |
|---|---|---|
| `CLAUDE_CODE_OAUTH_TOKEN` | 2026-06-21 | ✅ yes |

_No repository-level variables._

- **Environment `Preview`** secrets: none — ❌ **do NOT transfer; recreate in the destination**
- **Environment `Production`** secrets: none — ❌ **do NOT transfer; recreate in the destination**

**`craigoley/synthwatch-monitors`**

_No repository-level secrets._

_No repository-level variables._

- **Environment `copilot`** secrets: none — ❌ **do NOT transfer; recreate in the destination**

**Organization-scope (`craigoley`)** — org secrets/variables are shared across repos and **do NOT**
transfer with any single repo:

>  ⚠️ **GAP: github:org-secrets** — could not enumerate ORG-level secrets/variables for `craigoley` — the token lacks the `admin:org` scope (HTTP 403). ★ This is the highest-risk gap in this document: org secrets are invisible here AND do not transfer with a repo, so a migration that only accounts for repo secrets will silently lose them. Resolve with: `gh auth refresh -h github.com -s admin:org` then re-run this generator, or have an org owner run `gh api orgs/craigoley/actions/secrets`.


## 3. GitHub — workflows, jobs, protection, access

Workflow bodies are fetched from the GitHub API (the live default branch), not from a local clone,
so this reflects what actually runs.

```console
$ gh api repos/<repo>/contents/.github/workflows  →  parse each workflow's name / triggers / jobs
```


### 3.1 `craigoley/synthwatch` — workflows and job names

| Workflow | File | Triggers | Path filters | Job names |
|---|---|---|---|---|
| Auto-merge janitor | `automerge-janitor.yml` | schedule, workflow_dispatch | — | `reenable` |
| Browser smoke | `browser-smoke.yml` | pull_request, workflow_dispatch | runner/Dockerfile<br>runner/package.json<br>runner/package-lock.json<br>runner/scripts/browser-smoke.mjs<br>.github/workflows/browser-smoke.yml | `Browser smoke (launch in built image)` |
| CI gate | `ci-gate.yml` | pull_request | — | `ci-gate` |
| Claude review | `claude-review.yml` | pull_request | — | `Claude review`<br>`automerge` |
| CodeQL | `codeql.yml` | push, pull_request, schedule, workflow_dispatch | — | `Analyze (${{ matrix.language }})` |
| Dependency Review | `dependency-review.yml` | pull_request | — | `Review` |
| Deploy-script tests | `deploy-script-test.yml` | push, pull_request, workflow_dispatch | — | `Deploy-script tests` |
| Deploy runner to ACA Job | `deploy.yml` | push, workflow_dispatch | runner/**<br>db/**<br>!db/**/*.md<br>**/Dockerfile<br>.github/workflows/deploy.yml | `deploy` |
| ESLint | `eslint.yml` | push, pull_request, workflow_dispatch | — | `Lint` |
| Lib-flow parity | `libflow-parity.yml` | pull_request | — | `Lib-flow parity` |
| Mutation gate (PR) | `mutation-gate.yml` | pull_request | runner/**.ts<br>runner/scripts/mutation.sh | `mutation scope`<br>`mutation ${{ matrix.module }}` |
| Mutation nightly (full) | `mutation-nightly.yml` | schedule, workflow_dispatch | — | `nightly mutation ${{ matrix.module }}` |
| OSV-Scanner (PR) | `osv-scanner-pr.yml` | pull_request | — | `scan-pr` |
| OSV-Scanner | `osv-scanner.yml` | push, schedule, workflow_dispatch | — | `scan` |
| Playwright pairing | `playwright-pairing.yml` | pull_request, workflow_dispatch | — | `Playwright pairing (Dockerfile ⇄ npm)` |
| Schema-freeze pre-flight | `schema-freeze-preflight.yml` | pull_request | db/migrations/**<br>db/schema.sql | `Will this freeze synthwatch-api?` |
| OpenSSF Scorecard | `scorecard.yml` | branch_protection_rule, schedule, push, workflow_dispatch | — | `Scorecard analysis` |
| Self-heal | `self-heal.yml` | pull_request | — | `heal` |
| Semgrep | `semgrep.yml` | push, pull_request, schedule, workflow_dispatch | — | `Scan` |
| Test | `test.yml` | push, pull_request, workflow_dispatch | — | `Test (Node + Postgres)` |

_20 workflows, 22 jobs._

**★ Duplicate job-name check.** GitHub identifies a status check by its JOB NAME. Two jobs sharing
a name across workflows produce ambiguous required-status-checks, and a required check that matches
more than one job can block merges or pass on the wrong run.

✅ **No duplicate job names in `craigoley/synthwatch`** — every one of the 22 job names is unique, so each
status check resolves to exactly one job.

<sub>This check is not vacuous: `scripts/generate-handover-inventory.sh --self-test` feeds it a
known-duplicate fixture and asserts it FIRES, so a green line here means it looked and found none.</sub>

**Matrix jobs — the status-check name is DYNAMIC.** Each leg reports as its own check, so the
name to put in a required-checks list is the EXPANDED one, not the string in the YAML.

| Job (as written) | Defined in | Legs | Interpolates a matrix var? |
|---|---|---|---|
| `Analyze (${{ matrix.language }})` | codeql.yml:analyze | 2 | ✅ yes — legs get distinct names |
| `mutation ${{ matrix.module }}` | mutation-gate.yml:mutate | dynamic (computed at runtime) | ✅ yes — legs get distinct names |
| `nightly mutation ${{ matrix.module }}` | mutation-nightly.yml:mutate | 6 | ✅ yes — legs get distinct names |


**Branch protection & required checks (`main`)**

- Required status checks: `ci-gate`
- Required approving reviews: **0**
- Enforced for admins: false
- Dismiss stale reviews: false
- Rulesets: Code Quality Copilot review for default branch [active];Protect main [active]

**Collaborators and permission levels**

| User | Permission |
|---|---|
| `cao825` | admin |

### 3.2 `craigoley/synthwatch-api` — workflows and job names

| Workflow | File | Triggers | Path filters | Job names |
|---|---|---|---|---|
| Auto-merge | `auto-merge.yml` | pull_request | — | `Auto-merge (trusted authors)` |
| Build | `build.yml` | push, pull_request, workflow_dispatch | — | `Build (warnings as errors)` |
| CI gate | `ci-gate.yml` | pull_request | — | `ci-gate` |
| Claude Review | `claude-review.yml` | pull_request | — | `Claude review` |
| CodeQL | `codeql.yml` | push, pull_request, schedule, workflow_dispatch | — | `Analyze (${{ matrix.language }})` |
| Dependency review | `dependency-review.yml` | pull_request | — | `Review dependency changes` |
| Dependency scan | `dependency-scan.yml` | push, pull_request, schedule, workflow_dispatch | — | `dotnet list package --vulnerable` |
| Deploy API to Azure Functions | `deploy.yml` | push, workflow_dispatch | **/*.cs<br>**/*.csproj<br>host.json<br>.github/workflows/deploy.yml | `deploy` |
| Grant coverage | `grant-coverage.yml` | push, pull_request, workflow_dispatch | — | `Grant coverage (MI roles vs Bicep)`<br>`Grant coverage (Postgres table grants)` |
| Mutation gate (Stryker) | `mutation.yml` | push, pull_request | — | `stryker` |
| Schema parity | `schema-parity.yml` | push, pull_request, workflow_dispatch | — | `Schema parity (fixture vs runner migrations)`<br>`Runner column parity (fixture vs runner schema)` |
| Self-heal | `self-heal.yml` | pull_request | — | `heal` |
| Semgrep | `semgrep.yml` | push, pull_request, schedule, workflow_dispatch | — | `Scan` |
| Shell safety (SIGPIPE-grep) | `shell-safety.yml` | push, pull_request | — | `sigpipe-grep` |
| Test | `test.yml` | push, pull_request, workflow_dispatch | — | `Test (xUnit + Postgres service)` |
| Trace-signals parity | `trace-parity.yml` | push, pull_request, workflow_dispatch | — | `Trace-signals golden parity` |

_16 workflows, 18 jobs._

**★ Duplicate job-name check.** GitHub identifies a status check by its JOB NAME. Two jobs sharing
a name across workflows produce ambiguous required-status-checks, and a required check that matches
more than one job can block merges or pass on the wrong run.

✅ **No duplicate job names in `craigoley/synthwatch-api`** — every one of the 18 job names is unique, so each
status check resolves to exactly one job.

<sub>This check is not vacuous: `scripts/generate-handover-inventory.sh --self-test` feeds it a
known-duplicate fixture and asserts it FIRES, so a green line here means it looked and found none.</sub>

**Matrix jobs — the status-check name is DYNAMIC.** Each leg reports as its own check, so the
name to put in a required-checks list is the EXPANDED one, not the string in the YAML.

| Job (as written) | Defined in | Legs | Interpolates a matrix var? |
|---|---|---|---|
| `Analyze (${{ matrix.language }})` | codeql.yml:analyze | 2 | ✅ yes — legs get distinct names |


**Branch protection & required checks (`main`)**

- Required status checks: `ci-gate`
- Required approving reviews: **0**
- Enforced for admins: false
- Dismiss stale reviews: false
- Rulesets: Code Quality Copilot review for default branch [active];protect-main [active]

**Collaborators and permission levels**

| User | Permission |
|---|---|
| `cao825` | admin |

### 3.3 `craigoley/synthwatch-dashboard` — workflows and job names

| Workflow | File | Triggers | Path filters | Job names |
|---|---|---|---|---|
| Auto-merge | `auto-merge.yml` | pull_request | — | `Enable auto-merge (trusted authors only)` |
| CI gate | `ci-gate.yml` | pull_request | — | `ci-gate` |
| CI unstick | `ci-unstick.yml` | schedule, workflow_dispatch | — | `Approve or re-run parked runs (trusted PRs only)` |
| Claude Review | `claude-review.yml` | pull_request | — | `Claude review` |
| CodeQL | `codeql.yml` | push, pull_request, schedule, workflow_dispatch | — | `Analyze (${{ matrix.language }})` |
| Dependency review | `dependency-review.yml` | pull_request | — | `Review dependencies` |
| E2E | `e2e.yml` | push, pull_request, workflow_dispatch | — | `Playwright (hermetic)` |
| Enum coverage | `enum-coverage.yml` | push, pull_request, workflow_dispatch | — | `Enum coverage (runner schema vs consumer unions)` |
| ESLint | `eslint.yml` | push, pull_request, workflow_dispatch | — | `Lint` |
| OSV-Scanner (PR) | `osv-scanner-pr.yml` | pull_request | — | `scan-pr` |
| OSV-Scanner (scheduled) | `osv-scanner-scheduled.yml` | push, schedule, workflow_dispatch | — | `scan-scheduled` |
| Self-heal | `self-heal.yml` | pull_request | — | `heal` |
| Semgrep | `semgrep.yml` | push, pull_request, schedule, workflow_dispatch | — | `Scan` |
| SIGPIPE guard | `sigpipe-guard.yml` | push, pull_request, workflow_dispatch | — | `SIGPIPE guard (no fail-open piped grep -q)` |

_14 workflows, 14 jobs._

**★ Duplicate job-name check.** GitHub identifies a status check by its JOB NAME. Two jobs sharing
a name across workflows produce ambiguous required-status-checks, and a required check that matches
more than one job can block merges or pass on the wrong run.

✅ **No duplicate job names in `craigoley/synthwatch-dashboard`** — every one of the 14 job names is unique, so each
status check resolves to exactly one job.

<sub>This check is not vacuous: `scripts/generate-handover-inventory.sh --self-test` feeds it a
known-duplicate fixture and asserts it FIRES, so a green line here means it looked and found none.</sub>

**Matrix jobs — the status-check name is DYNAMIC.** Each leg reports as its own check, so the
name to put in a required-checks list is the EXPANDED one, not the string in the YAML.

| Job (as written) | Defined in | Legs | Interpolates a matrix var? |
|---|---|---|---|
| `Analyze (${{ matrix.language }})` | codeql.yml:analyze | 2 | ✅ yes — legs get distinct names |


**Branch protection & required checks (`main`)**

- Required status checks: `ci-gate`
- Required approving reviews: **0**
- Enforced for admins: false
- Dismiss stale reviews: false
- Rulesets: Code Quality Copilot review for default branch [active];protect-main [active]

**Collaborators and permission levels**

| User | Permission |
|---|---|
| `cao825` | admin |

### 3.4 `craigoley/synthwatch-monitors` — workflows and job names

| Workflow | File | Triggers | Path filters | Job names |
|---|---|---|---|---|
| Auto-merge | `auto-merge.yml` | pull_request | — | `Auto-merge (trusted authors)` |
| Check | `check.yml` | pull_request, push | — | `Check` |

_2 workflows, 2 jobs._

**★ Duplicate job-name check.** GitHub identifies a status check by its JOB NAME. Two jobs sharing
a name across workflows produce ambiguous required-status-checks, and a required check that matches
more than one job can block merges or pass on the wrong run.

✅ **No duplicate job names in `craigoley/synthwatch-monitors`** — every one of the 2 job names is unique, so each
status check resolves to exactly one job.

<sub>This check is not vacuous: `scripts/generate-handover-inventory.sh --self-test` feeds it a
known-duplicate fixture and asserts it FIRES, so a green line here means it looked and found none.</sub>

**Branch protection & required checks (`main`)**

- Required status checks: `Check`
- Required approving reviews: **1**
- Enforced for admins: false
- Dismiss stale reviews: true
- Rulesets: none

**Collaborators and permission levels**

| User | Permission |
|---|---|
| `cao825` | admin |
| `craig-oley-wegmans` | write |
| `cameron-perdue42` | write |

### 3.5 OIDC — the federated identity GitHub Actions deploys as

★ This principal is an **Entra app registration, NOT a resource in `synthwatch-rg`** — so it does **not**
move with the resource group and must be recreated (with its federated subjects) in the destination
tenant. Its `clientId` is the `AZURE_CLIENT_ID` repo variable above.

```console
$ az ad app federated-credential list --id <AZURE_CLIENT_ID>; az role assignment list --assignee <appId> --all
```

**App registration:** `synthwatch-gha-deployer` — appId `b92b0ccc-d24e-4b6f-a6a0-c94de476cf1a`

| Credential | Subject (who may assume it) | Issuer |
|---|---|---|
| `github-synthwatch-api-main` | `repo:craigoley/synthwatch-api:ref:refs/heads/main` | https://token.actions.githubusercontent.com |
| `github-main` | `repo:craigoley/synthwatch:ref:refs/heads/main` | https://token.actions.githubusercontent.com |

★ Only the subjects listed above can deploy. A repo NOT listed here has no path to Azure — check
this against the workflow list in 3.1 before assuming a repo can deploy itself.

**Its Azure role assignments:**

| Role | Scope |
|---|---|
| AcrPush | `/resourceGroups/synthwatch-rg/providers/Microsoft.ContainerRegistry/registries/synthwatcholey0620` |
| Contributor | `/resourceGroups/synthwatch-rg` |

## 4. Vercel (dashboard hosting)


```console
$ vercel project ls / vercel env ls   (requires the Vercel CLI + a token)
```

>  ⚠️ **GAP: vercel:cli** — the Vercel CLI is not installed and no VERCEL_TOKEN is present, so project / env-var / domain facts could NOT be enumerated live. ★ Do not fill this in from memory — that is the failure mode this document exists to end. Resolve with: `npm i -g vercel && vercel login && vercel link` in synthwatch-dashboard, then re-run. ★ What must be captured when resolved: env var NAMES per environment (production/preview/development), which are `NEXT_PUBLIC_*` (**inlined into the client bundle at BUILD time — changing one requires a REBUILD, not just a redeploy, and its value is publicly readable**), custom domains, and the protection-bypass setting.


**What is known from this side of the fence** (the runner's own view of the dashboard):

- Monitored Vercel host: `synthwatch-dashboard.vercel.app`

## 5. External dependencies with NO owner in this repo

★ Each of these is maintained by someone outside this codebase. In a new subscription or tenant they
do not come along, and a failure caused by one looks exactly like a parity failure.

| Dependency | Where it is wired | Who owns it | Migration risk |
|---|---|---|---|
| **Vercel protection-bypass token** | ACA secret `vercel-bypass-token` → env `VERCEL_BYPASS_TOKEN`; injected per-request by `runner/vercelBypass.ts`, host-scoped to `PROTECTED_BYPASS_HOSTS` | Wegmans (Vercel project owner) | Token is per-Vercel-project. Gates the commerce **PREVIEW** monitor; without it that check bot-blocks. |
| **Akamai / Wegmans IP allowlist** | Not in code — allowlisted on the Wegmans side, keyed to the egress IPs in §6 | Wegmans / Akamai ops | ★ **New subscription ⇒ new egress IPs ⇒ every authenticated Wegmans monitor bot-blocks until re-allowlisted.** Lead time is external. |
| **Algolia search endpoint** | Monitored directly as a check target (`*-dsn.algolia.net`) | Wegmans (Algolia app owner) | App ID is embedded in the check's URL; a key rotation on their side reds the check with no change here. |
| **OpenTable reservation widgets** | Third-party embed asserted by the Amore / Next Door monitors | OpenTable + the restaurant sites | Widget markup can change without notice; the monitor asserts their DOM, not ours. |
| **B2C test account** | ACA secret `cred-enc-key` decrypts `checks.login_credentials` | Wegmans identity team | Account lockout/expiry reds the authenticated flows; credentials must be re-seeded and re-encrypted in the destination. |

**Live third-party hosts asserted by enabled checks** (the real external surface):

- `example.com`
- `meals2go.com`
- `preview.commerce.wegmans.com`
- `qgppr19v8v-dsn.algolia.net`
- `synthwatch-api.azurewebsites.net`
- `synthwatch-dashboard.vercel.app`
- `wegapi.azure-api.net`
- `wegmansamore.com`
- `wegmans.com`
- `wegmansnextdoor.com`
- `www.meals2go.com`
- `www.wegmans.com`
- `www.wegmansnextdoor.com`


**★ First-party hosts with NO matching resource in this subscription**

These are monitored, passing, and ours by naming convention — but they are not in the resource list
in §1.1, and this subscription has only the one resource group. Whatever hosts them is OUT OF SCOPE
of everything above and must be tracked down separately before a migration is called complete.

| Host | Monitored by | Found in this subscription? |
|---|---|---|
| `synthwatch-api.azurewebsites.net` | checks 33, 4 | ✅ yes — `synthwatch-api` |
| `synthwatch-dashboard.vercel.app` | checks 222 | ⚠️ **NO — no resource named `synthwatch-dashboard` in any of the 1 resource group(s)** |

★ A ⚠️ row above means the component is hosted outside this subscription (a different subscription,
tenant, or a personal account). Moving `synthwatch-rg` will NOT move it, and the monitors that assert it
will keep passing right up until the day it disappears.

**Checks carrying encrypted login credentials** (each needs its account re-provisioned):

| Check | Name | Enabled |
|---|---|---|
| 353 | Wegmans B2C login — InfoSec on-demand unblock test | false |
| 355 | Wegmans: full authenticated pickup shopping flow | true |
| 396 | Wegmans Authorized User Add to Cart | false |

## 6. ★ Static egress IPs per region

★ **The single most migration-critical table in this document.** These are the addresses Wegmans/Akamai
allowlist. They are properties of the ACA managed ENVIRONMENT — a new subscription means new
environments and therefore **new IPs**. Monitors will then fail for an environmental reason that is
indistinguishable, from the logs, from a genuine parity regression.

Measured from what the runner ACTUALLY egressed as — `runs.egress_ip`, captured per run — not from
an environment property or a doc.

```console
$ psql "$DATABASE_URL" -c "SELECT location, egress_ip, count(*), min(started_at), max(started_at) FROM runs WHERE egress_ip IS NOT NULL AND started_at > now() - interval '30 days' GROUP BY 1,2"
```

| Region | Egress IP | Runs (30d) | First seen | Last seen |
|---|---|---|---|---|
| centralus | **`172.169.169.109`** | 88950 | 2026-07-01 | 2026-07-31 |
| eastus2 | **`20.85.72.149`** | 91612 | 2026-07-01 | 2026-07-31 |
| westus2 | **`20.80.135.196`** | 67454 | 2026-07-01 | 2026-07-31 |

_3 distinct (region, IP) pairs across 3 regions in the last 30 days._

★ **These IPs are empirically stable but NOT contractually stable.** The ACA environments are
Consumption-tier with no custom VNet, so outbound traffic leaves via Azure's shared egress, which
Microsoft explicitly documents as *not guaranteed stable* and *not intended as an allowlist target*.
They have held for the window above, and the allowlist depends on them — but a platform maintenance
event can change them with no deploy on our side. A durable answer requires VNet integration + a NAT
gateway, and `vnetConfiguration` is **immutable after environment creation**, so that means
re-creating all three environments — a migration-shaped change, not a config tweak.

---

## ⚠️ Gaps — what this run could NOT resolve

A named gap is worth more than a table that looks complete. Each line is something a reader must NOT
assume is covered.

- **github:org-secrets** — could not enumerate ORG-level secrets/variables for `craigoley` — the token lacks the `admin:org` scope (HTTP 403). ★ This is the highest-risk gap in this document: org secrets are invisible here AND do not transfer with a repo, so a migration that only accounts for repo secrets will silently lose them. Resolve with: `gh auth refresh -h github.com -s admin:org` then re-run this generator, or have an org owner run `gh api orgs/craigoley/actions/secrets`.
- **vercel:cli** — the Vercel CLI is not installed and no VERCEL_TOKEN is present, so project / env-var / domain facts could NOT be enumerated live. ★ Do not fill this in from memory — that is the failure mode this document exists to end. Resolve with: `npm i -g vercel && vercel login && vercel link` in synthwatch-dashboard, then re-run. ★ What must be captured when resolved: env var NAMES per environment (production/preview/development), which are `NEXT_PUBLIC_*` (**inlined into the client bundle at BUILD time — changing one requires a REBUILD, not just a redeploy, and its value is publicly readable**), custom domains, and the protection-bypass setting.

_2 unresolved item(s)._

---

_Generated `2026-07-31T12:13:59Z` · regenerate with `scripts/generate-handover-inventory.sh`._

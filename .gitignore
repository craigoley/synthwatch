# Dependencies
node_modules/

# Build output
dist/

# Local env / secrets — never commit
.env
.env.local
*.local

# Logs
*.log
npm-debug.log*

# Security scan artifacts (generated in CI)
*.sarif

# OS / editor
.DS_Store
.vscode/
.idea/

# Scratch analysis docs + scratch dirs — session working files, never committed (footgun: git add -A
# swept these into commits repeatedly). scratch/ is defensive here (no such dir today); it holds probe
# scaffolding in sibling repos.
ANALYSIS-*.md
scratch/
